# Module 클래스 구조

## 멤버 변수

- 소유하고 있는 파라미터 텐서 목록
- 소유하고 있는 하위 모듈 목록

## 메서드

- 순수 가상 함수인 forward(). (파생 클래스에서 구체적인 연산 흐름 정의)
- parameters(): 현재 모듈과 하위 모듈들이 가진 모든 가중치 텐서를 찾아 배열 형태로 반환. Optimizer 단계에서 사용됨.
